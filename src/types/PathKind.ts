/**
 * Enumeration of different types that can be a `Path`
 * @category path
 */
export enum PathKind {
  Unknown = 0,
  String = 1,
  Array = 2,
  PathWithBasePath = 3,
  PathMeta = 4,
  FlexiPath = 5
}
